spring.application.name=careercompass

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/careercompass 
spring.datasource.username=root
spring.datasource.password=sree29

# JPA / Hibernate
#Whenever we create a Java Entity (like User), Hibernate will automatically create the corresponding table in MySQL.
spring.jpa.hibernate.ddl-auto=update
#Whenever Spring executes SQL, you'll see it in the terminal.
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server Port
server.port=8080

jwt.secret=CareerCompassSecretKeyCareerCompassSecretKey12345
jwt.expiration=86400000